{
  "name": "test",
  "version": "1.0.0",
  "description": "Teste de Aplicação no Docker",
  "main": "index.js",
  "scripts": {
    "build": "npm install",
    "start": "npm cache clean && npm install --silent --progress=false && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas França <lucas.teleinfo@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.4",
    "mysql2": "^1.4.2",
    "sequelize": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^1.12.0"
  }
}
